import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { InsuredRelationService } from './services/insured-relation.service';

@NgModule({
    declarations: [],
    imports: [CommonModule],
    providers: [InsuredRelationService],
})
export class InsuredRelationModule {}
