export interface CreateSinister {
    reportNumber: string,
    manager: string,
    sinisterNumber: string,
    invoice: string,
    sinisterDate: string,
    resolutionDate: string,
    sinisterTypeId: number,
    location: string,
}
